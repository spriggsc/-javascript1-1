/**
 * Created by donaldspriggs on 8/4/15.
 */
// <!-- Prompt for a number value (provide a default of 10)
// Prompt for another number value (provide a default of 10)
// Convert the prompted values into integers.
// add the numbers together and alert the user with the outcome. -->


var num = 10;

var num2 = 10;

prompt(num);
prompt(num2);

parseInt(num + num2);
alert(num + num2);

